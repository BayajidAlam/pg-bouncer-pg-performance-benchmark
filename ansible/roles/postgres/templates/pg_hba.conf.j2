# PostgreSQL Client Authentication Configuration File
# Managed by Ansible

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     peer

# IPv4 local connections
host    all             all             127.0.0.1/32            md5

# Allow connections from monitoring server
host    all             all             {{ monitoring_server_ip }}/32    md5

# Allow connections from private subnet (if needed)
host    all             all             10.0.0.0/16             md5
